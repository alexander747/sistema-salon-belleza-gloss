# OpenSpec Configuration — pos-final
# Auto-generated by sdd-init | 2026-05-30

context: >
  Rebuild of pos-ok (salon SaaS). Node.js + Express + TypeScript + TypeORM + MySQL
  backend; React + TypeScript + Vite frontend. WhatsApp bot (Baileys), Google Gemini
  AI, Docker for n8n automation. Monorepo with backend/ and frontend/ directories.

strict_tdd: false
# Reason: No test runner detected in reference project (pos-ok). No jest, vitest,
# mocha, or other testing dependency in any devDependencies. TDD must be opted into
# once a runner is added.

testing:
  available: true
  runner: vitest
  framework: vitest
  layers:
    unit: true
    integration: true
    e2e: false
  coverage: true
  quality:
    linter: "eslint"     # available in frontend ref
    type_checker: "tsc"  # both backend and frontend
    formatter: null

phases:
  proposal: { required: true, auto_skip: false }
  spec: { required: true, auto_skip: false }
  design: { required: true, auto_skip: false }
  tasks: { required: true, auto_skip: false }
  apply: { required: true, auto_skip: false }
  verify:
    required: false
    auto_skip: true
    note: "Verify requires a test runner to be configured first"
  archive: { required: true, auto_skip: false }
